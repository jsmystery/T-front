query Brands($query: BrandQueryInput!) {
	brands(query: $query) {
		brands {
			id
			name
			slug
			logoPath
			rating
			reviewsCount
			about
			city
			category {
				name
				slug
			}
		}
		count
	}
}
