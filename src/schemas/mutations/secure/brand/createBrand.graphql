mutation CreateBrand(
   $input: CreateBrandInput!
) {
  createBrand(
      input: $input
  ) {
       # Specify the fields to return after the mutation is executed
    id
    name
    slug
    city
    logoPath
    about
    rating
  }
}
